{
  "name": "the-fallen",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "firebase": "^12.17.1",
    "phaser": "^3.60.0"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "vite": "^5.0.0"
  }
}
